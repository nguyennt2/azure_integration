# Core FastAPI dependencies
fastapi==0.115.0
uvicorn==0.30.6

# HTTP client for API calls
requests==2.32.3

# Data validation and settings management
pydantic==2.9.2

# Prometheus monitoring
starlette-prometheus==0.10.0
prometheus-client==0.21.0

# Logging (included in Python stdlib, but ensuring compatibility)
# No additional package needed, just here for clarity

# Optional: Development tools (commented out, uncomment if needed)
# pytest==8.3.3  # For unit testing
# httpx==0.27.2  # For async HTTP testing
